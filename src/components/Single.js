import React from 'react';
import TotalCase from './TotalCase';

const Single = ({name}) => {
    return (
        <div>
           {console.log(name)}
        </div>
    );
};

export default Single;